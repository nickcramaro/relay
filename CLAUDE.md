# Claude Code Instructions

## Version Management

Always keep `version` in `Cargo.toml` in sync with git tags. When creating a new release tag (e.g., `v0.1.3`), update `Cargo.toml` to match before tagging.
